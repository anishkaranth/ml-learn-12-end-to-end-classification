# Virtual envs
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/

# Jupyter
.ipynb_checkpoints/

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Local secrets (never commit)
.env
*.pem
credentials.json

# Keep artifacts/.gitkeep; ignore heavy regenerable junk if any
# (trained .joblib files ARE committed so clone+predict works)

# results/ is intentionally tracked (RESULTS.md, metrics.json, plots)

# results/ is intentionally tracked (RESULTS.md, metrics.json, JSON.shot, plots)
